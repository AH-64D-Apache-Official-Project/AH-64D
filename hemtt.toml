name = "AH-64D Apache Longbow"
prefix = "fza_ah64"
author = "Apache Project"
version = "0.0.1"

# Optional: Exclude fza_ah64_model from symlinks (same as build.json excludePboSymlinks)
# This will be handled in the SCons script instead

[header_exts]
version = "{{git \"id 8\"}}"

[[releasebuild]]
# For future release builds, creates a zip file with the mod
# Name pattern: fza_ah64_X.Y.Z.zip
releasebuild = "@zip fza_ah64_{{semver}}"
